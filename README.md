# LDUH - Linux Discord Update Helper

A lightweight Python-based background service for Debian-based distributions that automatically checks for and installs Discord updates.

## Features

- Auto-checks Discord updates at startup
- Downloads and installs the latest `.deb` package
- Launches Discord after updating
- Distro-agnostic desktop notifications
- Uses native `.deb` installation (no Snap/Flatpak)
